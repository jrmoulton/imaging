[package]
name = "imaging_vello_hybrid"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Vello hybrid (CPU/GPU via wgpu) backend for the imaging command stream."
readme = "README.md"
publish = false

[dependencies]
imaging = { features = ["std"], workspace = true }
imaging_wgpu = { default-features = false, features = ["wgpu-28"], workspace = true }
kurbo = { default-features = true, workspace = true }
peniko = { default-features = true, workspace = true }
vello_common = { default-features = true, version = "0.0.7" }
vello_hybrid = { default-features = true, version = "0.0.7" }
glifo = { path = "../../vello/glifo", default-features = true }
wgpu = { default-features = false, features = [
    "std",
    "wgsl",
], version = "28.0.0" }

[lints]
workspace = true

[dev-dependencies]
pollster = "0.4.0"
